OkResponse:
  content:
    application/json:
      schema:
        $ref: '#OkResponseBody'
OkResponseBody:
  required:
    - success
  properties:
    success:
      type: boolean
      example: true

